name: oil_gas_pipeline
version: "1.0.0"
config-version: 2

profile: oil_gas_pipeline

model-paths:      ["models"]
test-paths:       ["tests"]
seed-paths:       ["seeds"]
macro-paths:      ["macros"]
snapshot-paths:   ["snapshots"]

target-path:  "target"
clean-targets: ["target", "dbt_packages"]

models:
  oil_gas_pipeline:
    staging:
      +materialized: view
      +schema: public
    marts:
      +materialized: table
      +schema: public